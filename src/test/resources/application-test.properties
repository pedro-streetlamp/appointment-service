# Make tests fail fast if something is miswired.
spring.datasource.url=jdbc:postgresql://invalid-host/appointments

spring.liquibase.enabled=true

# jOOQ
spring.jooq.sql-dialect=postgres

# External services
doctor-calendar.base-url=http://localhost:8081
room-reservation.base-url=http://localhost:8082